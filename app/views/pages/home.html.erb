 <div class="banner container-aff">
      <div class="text-center">
        <%= form_tag search_path, method: :get do %>
          <div class="row">
            <div class="col-md-6">
              <%= text_field_tag :search, params[:search], placeholder: "Where do you want to ride", class: "form-control" %>
            </div>
            <div class="col-md-2">
              <%= submit_tag "Search", class: "btn btn-primary" %>
            </div>
          </div>
          <% end %>
      </div>
    </div>



<h1 class="text-center">CHOOSE YOUR BIKE !</h1>


    <div class="row">
      <% @bike.each do |b|  %>
      <div class="col-xs-12 col-sm-4">
      <%= link_to bike_path(b) do %>
            <div class="card">
              <% if !b.photos.empty? %>
              <span class="card-bg" style="background-image: url('<%= b.photos.first.image.url() %>')"></span>
              <% end %>
              <span class="card-shadow"></span>
              <span class="card-description">Popular</span>
              <span class="card-text">
                <h2> <%= b.category %></h2>
                <p> <%= b.title %></p>
              </span>
              <span class="card-user">
              <% if current_user %>
                <%= image_tag avatar_url(b.user), class: "img-circle avatar-medium" %>
              <% end %>
              </span>
            </div>
          <% end %>
          </div>
        <% end %>

<script>
$(function(){
  $("#start_date").datepicker({
    dateFormat: 'dd-mm-yy',
    minDate: 0,
    maxDate: "3m",
    onSelect: funtion(selected) {
      $('#end_date').datepicker("option", "minDate", selected);
      $('#end_date').attr('disabled', false);
    }
  });
  $("#end_date").datepicker({
    dateFormat: 'dd-mm-yy',
    minDate: 0,
    maxDate: "3m",
    onSelect: funtion(selected) {
      $('#start_date').datepicker("option", "maxDate", selected);
    }
  });
})
</script>
